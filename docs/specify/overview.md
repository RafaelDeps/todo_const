# Specify Overview

The `.specify` directory contains the core configuration, templates, and memory of the project's requirements and architecture.

## Folder Structure
- `memory/`: Permanent decisions and constitution.
- `templates/`: Blueprints for specs, plans, and tasks.
- `workflows/`: SDD cycle definitions.
